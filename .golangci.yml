run:
  timeout: 3m
linters:
  enable:
    - govet
    - errcheck
    - gosimple
    - ineffassign
    - staticcheck
    - gofmt
    - goimports
issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
